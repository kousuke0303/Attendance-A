<% provide(:title, "top") %>
<div class="center jumbotron">
  <h1>勤怠システム</h1>
  <% if logged_in? %>
    <button class="btn btn-lg btn-primary btn-login" disabled>ログイン</button>
  <% else %>
    <%= link_to "ログイン", login_path, class: "btn btn-lg btn-primary btn-login" %>
  <% end %>
  <p>アカウント作成は<%= link_to "こちら", signup_path %>から</p>
</div>