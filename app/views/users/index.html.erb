<% provide(:title, "All Users") %>
<% if params[:search] %>
  <h1>検索結果</h1>
<% else %>
  <h1>ユーザー一覧</h1>
<% end %>

<div class="row">
  <div class="col-md-10 col-md-offset-1">
    <div class="user-search">
      <%= form_with url: users_path, method: :get, local: true do |f| %>
        </p>ユーザー名検索</p>
        <%= f.text_field :search, placeholder: "キーワードを入力..." %>
        <%= f.submit "検索", class: "btn btn-sm btn-primary" %>
      <% end %>
    </div>
    
    <%= will_paginate %>
    <table class="table table-condensed table-hover" id="table-users">
      <thead>
        <tr>
          <th>名前</th>
          <th>基本時間</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= link_to user.name, user_path(user) %></td>
            <td><%= format_basic_info(user.basic_work_time) %></td>
            <td>
              <%= link_to "基本情報編集", edit_basic_info_user_path(user), class: "btn btn-primary" %>
              <% unless current_user.id == user.id %>
                <%= link_to "削除", user, method: :delete, class: "btn btn-primary",
                    data: { confirm: "#{user.name}を削除してよろしいですか？" } %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= will_paginate %>
  </div>
</div>